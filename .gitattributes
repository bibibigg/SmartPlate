# 모든 텍스트 파일의 줄 바꿈을 자동으로 처리하도록 설정합니다.
# Git 저장소에는 LF(Unix 방식)로 저장하고, 각 운영체제에 맞게 체크아웃 시 변환합니다.
* text=auto

# 특정 텍스트 파일들에 대해 명시적으로 줄 바꿈 처리를 지정합니다.
*.js text
*.jsx text
*.json text
*.md text
*.css text
*.html text